/**
 * The platform name users put under `platforms[].platform` in config.json.
 * MUST match `pluginAlias` in config.schema.json.
 */
export declare const PLATFORM_NAME = "Vera2";
/**
 * The npm package name. MUST match the `name` field in package.json.
 */
export declare const PLUGIN_NAME = "homebridge-vera2";
